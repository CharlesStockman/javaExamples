/**
 * Implementation of treemap
 *
 * Sort entries based on their natural ordering
 */
public class TreeMap {

    // Can put the values in any order and the keyset will be order
    // Can change the order by using TreeMap<K,V>(Comparator)
    // TreeMap does not use arrays

    // We can use map.firstKey() and map.lastKey() to find the small and largest value3NcNl->Nj
    // headMap(3) -- 1,2
    // tailMap(3) -- 3,4,5

}